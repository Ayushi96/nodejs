<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/output.css" />
  <title>Register Home</title>
</head>

<body class="bg-gray-50 min-h-screen font-sans">
  <%- include('../partials/navigation') %>

  <header class="text-center mt-8">
    <h1 class="text-3xl font-bold text-red-500 mb-2">Register Your Home</h1>
    <p class="text-gray-600">Fill in the details below to list your property</p>
  </header>

  <main class="flex justify-center mt-6">
    <form action="/host/add-home" method="POST" enctype="multipart/form-data"
      class="bg-white shadow-md rounded-lg p-8 w-full max-w-lg space-y-4">
      
      <input
        type="text"
        name="houseName"
        placeholder="Name of your house"
        required
        class="border border-gray-300 rounded-md px-4 py-2 w-full focus:outline-none focus:ring-2 focus:ring-red-400"
      />

      <input
        type="text"
        name="location"
        placeholder="Location"
        required
        class="border border-gray-300 rounded-md px-4 py-2 w-full focus:outline-none focus:ring-2 focus:ring-red-400"
      />

      <div class="flex gap-4">
        <input
          type="number"
          name="price"
          placeholder="Price per night"
          required
          class="border border-gray-300 rounded-md px-4 py-2 w-full focus:outline-none focus:ring-2 focus:ring-red-400"
        />

        <input
          type="number"
          name="rating"
          placeholder="Rating"
          min="0"
          max="5"
          step="0.1"
          class="border border-gray-300 rounded-md px-4 py-2 w-full focus:outline-none focus:ring-2 focus:ring-red-400"
        />
      </div>

      <input
        type="file"
        name="image"
        accept="image/*"
        class="border border-gray-300 rounded-md px-4 py-2 w-full file:mr-4 file:py-2 file:px-4 file:border-0 file:text-sm file:bg-red-400 file:text-white hover:file:bg-red-500 transition"
      />

      <button
        type="submit"
        class="bg-red-500 text-white w-full py-2 rounded-md hover:bg-red-600 transition font-semibold"
      >
        Submit
      </button>
    </form>
  </main>
</body>

</html>
